apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: shopkube
  labels:
    app: analytics-service
spec:
  type: ClusterIP
  selector:
    app: analytics-service
  ports:
    - port: 8088
      targetPort: 8088
      protocol: TCP
