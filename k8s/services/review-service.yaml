apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: shopkube
  labels:
    app: review-service
spec:
  type: ClusterIP
  selector:
    app: review-service
  ports:
    - port: 8087
      targetPort: 8087
      protocol: TCP
