apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: shopkube
  labels:
    app: cart-service
spec:
  type: ClusterIP
  selector:
    app: cart-service
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
